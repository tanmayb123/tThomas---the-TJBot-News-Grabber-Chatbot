/// <reference types="node" />
import { ICamera } from './interfaces';
import { DefaultCamera } from "./default";
export declare class SynchronizedCamera extends DefaultCamera implements ICamera {
    takePhoto: (fileName?: string) => Promise<Buffer>;
}
